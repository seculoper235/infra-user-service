spring:
  profiles:
    group:
      server: production
    active: server
---

# Spring
spring:
  config:
    activate:
      on-profile: production
  devtools:
    add-properties: false
  output:
    ansi:
      enabled: always
